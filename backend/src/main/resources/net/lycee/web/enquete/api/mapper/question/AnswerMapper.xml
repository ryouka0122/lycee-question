<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.lycee.web.enquete.api.mapper.question.AnswerMapper">

    <insert id="insert">
        INSERT INTO QES_ANSWER (
            QUESTION_ID,
            ANSWER_ID,
            ANSWER_NO,
            DESCRIPTION
        ) VALUES (
            #{record.questionId},
            #{record.answerId},
            #{record.answerNo},
            #{record.description}
        )
    </insert>

</mapper>